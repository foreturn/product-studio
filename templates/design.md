---
schema: 4
memory: design
scope: current-project-code
project_root: "."
updated_at: ""
---

# design 代码事实

<!-- 每次成功编码终态都执行记忆同步检查。只收录已由当前代码与适用验证支持、预计后续仍会使用，且具有跨消费者、重查成本或误判风险的总结性事实；不按任务、文件或 Symbol 机械建卡。实例化后填写带时区时间，删除本说明、事实键目录、示例与所有占位；无事实变化时报告 memory: 0 keys changed，不改文件与 updated_at。语义不变时原位更新，事实消失时删除，最后一张事实删除后移除本文件；Git 承担历史。 -->

## 事实键

- `design:journey:<slug>`：代码实际支持的核心用户任务、状态反馈与恢复路径。
- `design:rule:<slug>`：已实现的业务规则、例外和权限差异。
- `design:boundary:<slug>`：实际模块或领域边界、数据所有权与权威写入责任。
- `design:contract:<slug>`：跨模块、前后端或外部系统间的共享契约与兼容关系。
- `design:invariant:<slug>`：实现必须维持的一致性、安全、可靠性或性能不变量。
- `design:migration:<slug>`：当前仍生效的兼容阶段、数据演进顺序与不可逆边界。

## 当前代码事实

### design:contract:order-creation

- **事实摘要**：<代码已经执行的一项旅程、规则、边界、契约或不变量，以及未来修改不得破坏的关键约束>
- **代码定位**：<一至三个 `仓库相对路径#精确位置`，精确位置使用权威 Symbol、契约、路由或 Schema>
- **依赖与影响**：<直接依赖该事实的模块、接口、页面、外部消费者或相关事实键；无则写“无”>
- **验证入口**：<可重复使用的测试、脚本、命令或检查旅程，关键断言与稳定未覆盖边界>
- **失效条件**：<哪些产品契约、所有权、边界、数据流或实现变化后必须重读代码并同步本卡>
