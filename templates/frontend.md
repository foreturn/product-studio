---
schema: 4
memory: frontend
scope: current-project-code
project_root: "."
updated_at: ""
---

# frontend 代码事实

<!-- 每次成功编码终态都执行记忆同步检查。只收录已由当前代码与适用验证支持、预计后续仍会使用，且具有跨消费者、重查成本或误判风险的总结性事实；不逐样式值、组件、文件或 Symbol 机械建卡。实例化后填写带时区时间，删除本说明、事实键目录、示例与所有占位；无事实变化时报告 memory: 0 keys changed，不改文件与 updated_at。语义不变时原位更新，事实消失时删除，最后一张事实删除后移除本文件；Git 承担历史。 -->

## 事实键

- `frontend:surface:<slug>`：核心界面任务、入口、信息层级与结果。
- `frontend:component:<slug>`：多消费者共享的组件契约。
- `frontend:state:<slug>`：异步状态、服务端映射与恢复行为。
- `frontend:layout:<slug>`：稳定容器、网格、滚动与层级约束。
- `frontend:responsive:<slug>`：重排、溢出、触控和虚拟键盘约束。
- `frontend:a11y:<slug>`：核心旅程的语义、键盘、焦点和通知约束。
- `frontend:system:<slug>`：多消费者共享的令牌和设计系统规则。

## 当前代码事实

### frontend:state:order-create

- **事实摘要**：<真实界面已经执行的一项任务、状态转换、恢复行为、布局或共享设计约束>
- **代码定位**：<一至三个 `仓库相对路径#精确位置`，精确位置使用权威路由、组件、令牌、hook、store 或样式 Symbol>
- **依赖与影响**：<直接依赖的页面、组件、请求、状态拥有者、下游复用者或相关事实键；无则写“无”>
- **验证入口**：<可重复使用的测试、浏览器旅程或检查入口，关键状态、视口、断言与稳定未覆盖边界>
- **失效条件**：<哪些组件、令牌、路由、接口、状态机、断点或浏览器支持变化后必须同步本卡>
