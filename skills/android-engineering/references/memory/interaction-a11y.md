# Android 工程项目记忆 · 表单、交互反馈与可达性

- 记住关键表单的字段含义、默认值、依赖与草稿所有者，本地格式校验和权威服务裁决的分界，以及字段错误、整体错误与输入修正之间的关系。
- 记住提交防重、真实业务完成、失败保留和取消恢复的约定，各类加载、刷新、空、拒绝、失败与部分成功对应的反馈位置、持续时间和下一步；状态与错误分类引用其权威事实。
- 记住表单、列表、表格和批量操作中的数值、空值、筛选排序、跨页选择、影响范围与部分失败语义，以及常用动作的名称、顺序、位置和触控、键盘、辅助操作的等价入口。
- 记住应用语言的来源与生效时机、消息和格式化资源的版本、复数与双向文本规则，以及日期、数字、货币、时区和混合方向标识在恢复与切换时的稳定语义。
- 记住公共交互的名称、角色、值、状态、焦点顺序、错误关联和动态播报，高对比、减少动画、字体放大、TalkBack、键鼠与替代控制下必须等价保留的核心任务。
