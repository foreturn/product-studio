---
name: frontend
description: 设计、实现并真实渲染验证 Web 或应用界面的任务可用性、信息架构、接口集成、组件与视觉、交互状态、响应式布局、可访问性、安全与性能。适用于页面、路由、表单、列表、导航、组件、样式、设计令牌、请求适配、前端状态、移动适配和用户可见行为；产品旅程未定时使用 design，系统边界问题交给 architecture，接口问题交给 backend，跨技能编排使用 router。
---

# 前端工程与体验

以已确认的用户任务和接口契约为准，只作最小必要修改；不得以视觉偏好改写业务规则。

## 输入门禁

- 明确当前项目根、仓库指令、目标用户、真实入口、核心任务、成功结果和不得破坏的现有能力。
- 读取相关产品事实、存在时的 `docs/product-studio/frontend.md` 中与目标页面、路由、接口适配、令牌、组件、布局、状态、安全和性能有关的总结性代码事实，以及现有设计系统、路由、组件、样式和请求层。
- 核对 API 请求、响应、错误、权限、分页、幂等和兼容行为；接口语义不清时交给 `$backend`，不得在前端猜造服务端规则。
- 用户、旅程、业务闭环、范围或体验目标未定时调用 `$design`；跨模块系统边界、数据所有权、共享不变量、信任边界或系统级质量约束未定时交给 `$architecture`；具体接口字段、错误、权限和一致性实现未定时交给 `$backend`。仅限前端内部的组件与状态边界由本技能裁定。
- 同时牵涉前后端共享契约时由 `$router` 选择最小代码技能链；单一局部前端改动可直接执行本技能并交 `$verification` 验收，常规终端验收本身不触发 `$router`。

## 专业能力

先查看 [前端设计与用户体验专业准则](references/principles.md) 的“目录”与“核心能力”，完整读取本次命中的能力段落；涉及整体体验、交付收口或风险复核时，再读“常见误判”。

## 实施流程

按实际改动裁剪步骤；纯令牌、样式或响应式修复只追踪相关源码、消费者与可见不变量，并作聚焦渲染验证，不扩成页面重设计或接口重审。

1. 从与改动直接相关的真实入口追踪页面、路由、组件、样式、请求和状态调用链，核对本次会改变或必须保持的消费者与当前证据。
2. 定位仍待实现的前端行为，完整读取命中的能力准则；产品语义未定时退回 `$design`，跨边界决定未定时退回 `$architecture`，具体接口语义不清时退回 `$backend`。
3. 沿用项目现有技术栈、设计系统和请求层实现最小完整改动，同步受影响的路由、类型、适配器、组件、令牌与测试；没有稳定消费者或风险证据的公共抽象不进入实现。
4. 运行项目既有的静态、类型、代码检查、聚焦测试和构建，再按命中能力与风险在真实浏览器、目标视口和边界状态中完成核心任务与恢复路径。
5. 核对控制台、网络、资源、布局、可访问性、性能、最终差异和全部消费者，区分自动化、模拟、截图与真实操作证据，再按“输出与交接”移交。
6. 在终态回复前执行记忆同步检查，报告实际增改删的事实主题；没有总结性事实变化时报告 `memory: 0 facts changed`。

## 当前代码事实记忆

开始工作时，若 `<当前项目根>/docs/product-studio/frontend.md` 存在，只读取命中的事实主题及直接关联。每次编码任务收口前完整读取 [frontend 记忆规则与实例格式](references/memory.md)，仅按最终代码同步 `frontend` 自己的事实册；首次确有事实时使用其中格式，不得预建空册。重构、迁移和变更过程不入册；没有总结性事实变化时报告 `memory: 0 facts changed`。

## 输出与交接

- 符合现有技术栈与设计系统的页面、路由、组件、样式、状态、请求适配和聚焦测试，以及信息架构、接口映射、令牌、组件变体、交互状态、响应式、可访问性、安全与性能决定的实现说明。
- 真实浏览器、视口、状态、数据、控制台和网络证据，以及执行过的检查和已知缺口。
- 向 `$verification` 交付真实入口、身份、数据、状态矩阵、浏览器／视口要求和复现步骤。
- 实现发现系统边界、数据所有权或共享不变量冲突时退回 `$architecture`；发现具体 API 字段、错误或权限实现不清时退回 `$backend`。

## 完成门禁

- 从真实入口完成已确认核心任务，所有命中能力准则均有实现与相称证据；不适用项具有产品、接口或运行边界依据。
- 项目要求的静态、类型、测试与构建检查通过，目标浏览器、视口、状态和高价值恢复路径取得真实操作证据；无法取得的证据明确标为缺口。
- 最终差异只含授权范围，没有猜造产品或接口语义，没有无消费者抽象、矛盾状态或未说明的安全与兼容风险。
- 消费者、交接材料与当前事实记忆已按最终代码核对，自动化、模拟、截图和真实浏览器证据没有互相冒充。

## 边界

- 可在已确认产品和接口契约内裁决信息层级、接口映射、交互反馈、组件边界、令牌使用、响应式布局、可访问性、前端安全和性能实现。
- 不自行发明产品范围、业务规则、权限模型、接口错误或服务端一致性语义；产品与体验目标缺口交给 `$design`，系统架构缺口交给 `$architecture`，具体接口缺口交给 `$backend`。
- 新依赖、公共组件或设计令牌必须有现有方案不足和真实消费者证据，不为未来预留抽象。
- 本技能止于前端代码和浏览器验证，不执行外部环境操作；是否通过由 `$verification` 独立裁决。
